The following packages must be installed to run the project:
npm install -g create-react-app
npm install pm2 -g

To run the project, you need to apply the command:
npm install

Link to the running project: http://skinbuy.ru:3001/